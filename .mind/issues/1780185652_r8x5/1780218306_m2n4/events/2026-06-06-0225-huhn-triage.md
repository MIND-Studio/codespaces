---
id: 01JZW0B2R0HUHN00TRIAG0157
kind: triage
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0225Z
to: todo
priority: high
---

**Unblocked.** #142 (`pr-pod-native-turtle`, the delegated pod-native write path this reuses)
is now `done`. The design is settled in
[ADR-0002](../../../../docs/adr/0002-membership-bridge-mediated-capability.md): membership is a
bridge-enforced capability — pod-native `members.ttl` (WebID→role: reader/writer/admin) in the
owner's pod, `requireOwner` → `requireMember(repo, minRole)`, member `Read` WAC grant only on
private repos, bridge stays sole writer via `getOwnerFetch`.

Moving to `ready-for-agent` — an agent may implement per the ADR. Note `mc:afk false` still
holds (touches auth + WAC), so run it with a human in the loop.
