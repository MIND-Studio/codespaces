---
id: 01JZW0B5V0HUHN00CLOSE0157
kind: close
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0248Z
from: review
to: done
prev: 01JZW0B4T0CLAUDE0HAND20157
---

Reviewed and landing (auth/WAC-sensitive — reviewed with care). Both ACs are met:
`members.ttl` records `WebID → role` (`src/lib/solid/members.ts`, round-tripped in
`tests/members-roundtrip.test.ts`), and `addMember` grants the matching pod ACLs — on a
private repo the member gets `acl:Read` on `pulls/` + the roster doc (`setMemberReadAcl`),
public repos unchanged. `requireMember` lands as the enforcement primitive per
[ADR-0002](../../../../docs/adr/0002-membership-bridge-mediated-capability.md). `tsc`
clean, 94 tests green.

Accepting the agent's scope call: the ~30 existing `requireOwner` collab routes are **not**
mass-rewired to `requireMember` here — that's a per-route auth-surface change I want to land
deliberately. Filed-in-mind as a follow-up (along with a Settings → Members UI and live-CSS
member-read enforcement). This issue's two ACs are done; the epic continues with the route
migration.
