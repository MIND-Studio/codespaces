---
id: 01JZW0B1Q0ALICE0OPEN0157
kind: triage
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0155Z
to: todo
labels: [area:auth]
---

**Design decision made — `needs-design` gate lifted.** Membership is a
**bridge-enforced capability**, not a pod WAC write-grant: a pod-native
`members.ttl` (WebID→role) in the owner's pod, read by the bridge, which writes as
the owner via `getOwnerFetch`; the only real WAC change is a member Read grant on
private repos. Rejected direct WAC delegation (breaks the bridge-as-sole-writer
invariant, non-atomic revocation, cross-pod CORS/OIDC). Recorded in
[`docs/adr/0002-membership-bridge-mediated-capability.md`](../../../../docs/adr/0002-membership-bridge-mediated-capability.md).

Dropped the `needs-design` label. **Still blockedBy #142** (`pr-pod-native-turtle`,
the delegated pod-native write path) — this reuses it. Moving to `needs-triage`
rather than `ready-for-agent`: once #142 reaches `done`, triage this to
`ready-for-agent` to implement per the ADR.
