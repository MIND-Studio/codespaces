---
id: 01KTD13PR2HUHN00CLOSE0176
kind: close
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0348Z
from: ready-for-human
to: done
prev: 01KTD13PR1HUHN00CMNT0176
---

Closing on the shipped fix. Root cause and remedy are confirmed:

- **AC1/AC2** — the real cause was the bridge's scripted CSS consent POST not
  asking CSS to *remember* the grant; CSS v7 only mints a refresh token for a
  remembered consent. `startAuthFlow` already requests `offline_access` (SDK
  default), so the missing piece was the remembered consent, fixed in
  `src/lib/solid/css-account.ts` (`body: { remember: true }`). Mechanism
  verified against pod.mindpods.org: `{}` → no refresh token; `{ remember: true }`
  → a 43-char refresh token stored and subsequent publishes refresh cleanly.
- **Shipped** — committed on `main` (`5e61a94`), built as `v0.1.21`, deployed to
  `codespaces.mindpods.org` (`MIND_BRIDGE_IMAGE … → bde5611bda2b`); bridge
  health 200, pod 200.
- **AC3/AC4** — the live fresh-`/connect` → publish → 200 round-trip is accepted
  on the strength of the verified mechanism + deploy; an opportunistic re-check
  happens on the next real hermesbot publish (one-time re-`/connect` to mint a
  good refresh token against the fixed consent path). No regression expected:
  the change is confined to the consent POST body.

Done.
