---
id: 01JZWF9Q1HUHN000CLOSE0173
kind: close
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0247Z
from: ready-for-human
to: done
---

Reviewed (auth-sensitive — reviewed with care). A dead delegated registration is now
detected and surfaced, not silent: the SDK's `invalid_client`/non-logged-in throws are
normalised to `OidcRefreshFailedError` (`src/lib/solid/oidc-server.ts:219-265`), which
`getOwnerFetch` turns into an `OwnerFetchUnavailableError("needs-reauthorization")`
pointing at `/connect` and — correctly, per P0-R2 — does **not** fall back to seeded creds
when a stale delegated identity exists (`src/lib/solid/fetch-for-owner.ts:51-60`). Covered
by `tests/oidc-refresh-normalize.test.ts` + `tests/fetch-for-owner.test.ts`. Done.
