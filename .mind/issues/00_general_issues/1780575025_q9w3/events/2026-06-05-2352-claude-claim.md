---
id: 01JZWF9Q0A0CLAUDE0INPR173
kind: claim
actor: "http://localhost:3011/claude/profile/card#me"
actorKind: agent
at: 2026-06-05T2352Z
to: doing
ttl: PT2H
---

Claimed. Normalizing a *thrown* OIDC refresh failure (dead client registration / `invalid_client` after a `.css-data` wipe) into `OidcRefreshFailedError` in `loadAuthedFetchForWebId`, so it surfaces as the existing `needs-reauthorization` → `/connect` 503 instead of an opaque 500. Adding the missing regression test.
