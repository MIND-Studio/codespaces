---
id: 01JZWX10B1HUHN00CLOSE0150
kind: close
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0247Z
from: review
to: done
---

Reviewed (auth-sensitive — reviewed with care). The fix is in: per-session DPoP keypair +
refresh token persist AES-256-GCM-encrypted in SQLite via `makeIdentityStorage`
(`src/lib/registry/identities.ts:73-131`), so the SDK rehydrates them from storage after a
process restart rather than relying on in-memory state. Verified by
`tests/identity-storage-restart.test.ts` ("identity storage survives a bridge restart").
No more re-connect loop on restart. Done.
