---
id: 01JZX00000CLAUDE0INPR160
kind: claim
actor: "http://localhost:3011/claude/profile/card#me"
actorKind: agent
at: 2026-06-06T0007Z
to: doing
ttl: PT2H
---

Claimed. Building: (1) a pod tracker writer in `tracker-build` that folds `.mind/` into a
multi-doc `flow:Tracker` (`state.ttl` + `epics.ttl` + `tracker.ttl`) under
`{podRoot}/codespaces/{repo}/.mind/` via the delegated owner fetch, public-read ACL;
(2) the dashboard Issues surface reading that `flow:Tracker` (grouped by epic, General
bucket), pod copy as source; (3) the Registry index as a rebuildable projection. Mapping
the current issue-render + pod-write + tracker-build subsystems first.
