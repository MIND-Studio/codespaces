---
id: 01JZX00001HUHN00CLOSE0160
kind: close
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0247Z
from: review
to: done
---

Reviewed and landing. All six ACs are verified in code: the Issues UI renders the
`.mind`-derived `flow:Tracker` grouped by epic with a "General" bucket
(`src/app/repos/[owner]/[repo]/issues/page.tsx`, `src/lib/tracker/model.ts:108-127`),
read **pod-first** with a git-blob fallback (`src/lib/tracker/source.ts:27-35`,
`src/lib/solid/tracker-pod.ts:128-140`); the post-receive hook mirrors the trio into the
pod (`publishTrackerToPod`) and projects the Registry index from it
(`src/lib/registry/issue-projection.ts`); public-read ACL keeps mind-issues rendering the
same URL. Covered by `tests/tracker-pod.test.ts` + `tests/issue-projection.test.ts`.
Live-CSS integration stays in the §3.2 backlog per project convention. Done.
