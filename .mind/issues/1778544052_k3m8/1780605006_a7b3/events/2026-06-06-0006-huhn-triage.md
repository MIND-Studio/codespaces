---
id: 01JZX00000HUHN000RFA0160
kind: triage
actor: "https://huhn.example/profile/card#me"
actorKind: human
at: 2026-06-06T0006Z
to: ready-for-agent
---

Design gate resolved (the three "open questions" are decided):
1. **Scope** — full feature in one PR (all 6 acceptance criteria).
2. **Pod doc model** — multi-doc, append-only (`state.ttl` + `epics.ttl` + `tracker.ttl`),
   matching `.mind`'s emit and safe for concurrent human+agent writers.
3. **Location/ACL** — `{podRoot}/codespaces/{repo}/.mind/`, public-read (same ACL shape as
   `issues/`).

Flat `issue.ttl` store: keep as a back-compat read for existing seeded repos (alice/notes
#1/#2), with the `.mind`-derived `flow:Tracker` as the canonical render. Moving to
ready-for-agent so the build can proceed.
